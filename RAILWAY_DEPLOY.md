# 🚂 Деплой на Railway - Пошаговая инструкция

## ✅ Что уже сделано

- ✅ Структура проекта исправлена
- ✅ `bot.py` в корне (точка входа)
- ✅ `requirements.txt` оптимизирован
- ✅ `runtime.txt` добавлен (Python 3.11.9)
- ✅ `.env.example` создан

---

## 📋 Шаг 1: Залей код на GitHub

```bash
git add .
git commit -m "Fix: Railway deployment structure"
git push origin main
```

---

## 🚂 Шаг 2: Создай проект на Railway

1. Зайди на [railway.app](https://railway.app)
2. Нажми **"New Project"**
3. Выбери **"Deploy from GitHub repo"**
4. Выбери репозиторий: `3Ve3Daa/peachmine-broadcast`
5. Railway автоматически определит Python проект

---

## ⚙️ Шаг 3: Настрой переменные окружения

В Railway панели → **Variables** → добавь:

```
TOKEN=your_discord_bot_token_here
GUILD_ID=your_guild_id_here
ADMIN_IDS=820246873297649664,985239713751384094
CLIENT_ID=your_client_id_here
DISCORD_INVITE=https://discord.gg/your_invite
```

**ВАЖНО:** 
- Не используй кавычки! Просто значения.
- `ADMIN_IDS` - несколько ID через запятую (можно добавить больше админов)

**Где взять значения:**
- Скопируй из файла `peachmine-broadcast/.env`
- Или используй свои реальные значения

---

## 🚀 Шаг 4: Настрой команду запуска

В Railway панели → **Settings** → **Deploy**:

### Start Command:
```
python bot.py
```

### Root Directory:
```
(оставь ПУСТЫМ - не указывай ничего)
```

### Build Command:
```
(оставь по умолчанию или пусто)
```

---

## 🔄 Шаг 5: Redeploy

1. Нажми **"Redeploy"** в Railway
2. Дождись завершения билда (1-2 минуты)
3. Проверь логи - должно быть:
   ```
   🍑 Запуск PeachMine » Рассылка...
   ══════════════════════════════════════════════════
     🍑 PeachMine » Рассылка
   ══════════════════════════════════════════════════
     Бот: YourBotName#1234
   ```

---

## ✅ Проверка работы

1. Зайди в Discord сервер
2. Используй команду `/info`
3. Бот должен ответить в ЛС

---

## 🐛 Если что-то не работает

### ❌ "Error creating build plan"
- Убедись что `bot.py` и `requirements.txt` в **корне** репозитория
- Проверь что Root Directory **ПУСТОЙ**

### ❌ "Module not found"
- Проверь `requirements.txt` - должны быть только:
  ```
  py-cord==2.6.1
  python-dotenv==1.0.0
  ```

### ❌ "TOKEN not found"
- Проверь Variables в Railway
- Убедись что переменная называется `TOKEN` (без кавычек)

### ❌ Бот не отвечает
- Проверь что GUILD_ID правильный
- Проверь что бот добавлен на сервер с правильными правами

---

## 📊 Логи Railway

Чтобы посмотреть логи:
1. Railway панель → **Deployments**
2. Выбери последний деплой
3. Нажми **"View Logs"**

---

## 🔄 Обновление бота

После изменений в коде:

```bash
git add .
git commit -m "Update bot"
git push origin main
```

Railway автоматически задеплоит новую версию!

---

## 💡 Полезные команды

### Локальный запуск (для тестов):
```bash
python bot.py
```

### Проверка зависимостей:
```bash
pip install -r requirements.txt
```

---

## 📞 Поддержка

Если проблемы остались - проверь:
1. ✅ Все переменные в Railway Variables
2. ✅ Start Command: `python bot.py`
3. ✅ Root Directory пустой
4. ✅ `bot.py` в корне репозитория

---

**Готово! Бот должен работать 24/7 на Railway! 🎉**
